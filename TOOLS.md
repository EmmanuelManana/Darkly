```bash 

VULNERABILITY_SCANNING_TOOLS___
    => Before The exploits, one has to find where the site 
    is vulnerable and how the API is set.
    => That is one has to crawl/spider the Wep App, using Tools such 
    as BurpSuite OR OWASP ZAP.